// import bip38 from 'bip38'

// export const encrypt = (key: string, compressed: boolean, passphrase: string) => bip38.encrypt(key, compressed, passphrase)
// export const decrypt = (encryptedKey: string, passphrase: string) => bip38.decrypt(encryptedKey, passphrase)
