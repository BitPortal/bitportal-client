import React from 'react'
import { View } from 'react-native'

const DappFooterTableViewCell = (props) => {
  return (
    <View style={{ flex: 1, justifyContent: 'space-between', flexDirection: 'row', alignItems: 'center', backgroundColor: 'white' }} />
  )
}

export default DappFooterTableViewCell
