/* eslint-disable */

import React from 'react'
import PropTypes from 'prop-types'

class CollectionViewItem extends React.Component {
  render() {
    // These items don't get rendered directly.
    return null
  }
}

CollectionViewItem.propTypes = {
  reactModuleForCollectionViewCell: PropTypes.string
}

export default CollectionViewItem
